# 복습 중인 용어/개념

## 2026-02-16 (자료구조)

- **dict.get(키, 기본값)**: 딕셔너리에서 키가 없을 때 에러 대신 기본값 반환. `sensor.get("status", "미설정")`.
- **집합 연산**: `a & b`(교집합), `a | b`(합집합), `a - b`(차집합). 알람 분석에서 "연속 알람", "신규 알람" 구할 때 유용.

## 2026-02-17 (객체지향 프로그래밍)

- **self**: 메서드 안에서 "나 자신(객체)"을 가리키는 변수. 정의할 때만 적고 호출할 때는 안 넣음.
