# Codex compatibility shim

This project keeps agent instructions in `CLAUDE.md`.
Always read and follow `CLAUDE.md` in this directory as the source of truth.
If both files are present, `CLAUDE.md` wins.
